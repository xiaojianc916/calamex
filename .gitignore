node_modules
dist
.DS_Store
.env
.env.*
!.env.example
.idea
.vscode
src-tauri/target
target
*.log
*.bak
.strands-sessions
builtin-agent/.strands-sessions
builtin-agent/.builtin-agent/

# 构建产物 / 缓存
*.pdb
*.tsbuildinfo
.node-compile-cache/
.mastra/

# vite 配置的编译产物（源文件为 vite.config.ts）
/vite.config.js
/vite.config.d.ts

# 测试与性能报告产物
test-results/
playwright-report/
*.report.html

src-tauri/resources-bundle/

# Tauri 自动生成的 ACL schema（每次构建会重新生成，不应提交）
src-tauri/gen/schemas/

# Compiled JS artifacts that shadow .ts sources (never used at runtime)
builtin-agent/src/**/*.js

# 运行时 SQLite 产物（绝不提交）
builtin-agent/.agent-sidecar/
